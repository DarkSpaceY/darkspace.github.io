---
const defaultLocale = "en";
const redirectUrl = `/${defaultLocale}/about/`;
---
<meta http-equiv="refresh" content={`0;url=${redirectUrl}`} />
<script define:vars={{ redirectUrl }}>
  window.location.replace(redirectUrl);
</script>
<a href={redirectUrl}>Enter</a>